# internal-admin

Deployable composition root. Product/domain logic belongs in `domains/` and technical infrastructure in `platform/`.

    ## Platform role
    StrideLab staff tooling. Must use explicit least-privilege support/admin capabilities.

