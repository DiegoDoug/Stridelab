# Missing optional dependencies

The following names from the request were not present as standalone installed skills in the inspected Work skill catalog or as standalone skills in the SKILL Maker reference packages:

- `ios-hig-design` — Apple depth is available as a bounded adapter inside the existing general `visual-ui-design` reference package.
- `interface-design` — continuity/Design Memory behavior is available as an attachment inside the existing general `visual-ui-design` package.
- `taste-skill` — taste-regression behavior is available as an evaluation attachment, not a top-level trigger.
- `ui-ux-pro-max` — knowledge retrieval behavior is available as a bounded attachment, not a top-level trigger.
- `impeccable` — visual-quality-gate behavior is documented as an optional attachment; no standalone skill was available.
- `artifact-template-system-design`
- `artifact-template-strategy-memorandum`
- `artifact-template-legal-memorandum`
- `artifact-template-design-report`
- `artifact-template-financial-budget`

No manifest declares these as required. Their absence does not block the core package, and no unavailable skill was fabricated.
