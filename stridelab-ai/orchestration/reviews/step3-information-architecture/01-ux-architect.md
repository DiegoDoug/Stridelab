# Step 3 review — `ux-architect` (self-check against the `information-architecture` SKILL contract)

- **Scope:** `docs/product/information-architecture.md` in full — traceability (§1), object inventory (§2), hierarchy model (§3), destination map (§4), role walkthroughs (§5), cross-linking (§6), search (§7), platform continuity (§8), findability validation (§9).
- **Authoritative inputs:** `information-architecture` SKILL contract (`stridelab-ai/departments/01-product-experience/skills/core/information-architecture/SKILL.md`); `feature-prioritization.md`, `mvp-release-scope.md` (both APPROVED); `domain-model.md` (APPROVED).
- **Evidence inspected:** the SKILL's required output-contract items (content/object inventory, hierarchy model, destination map, navigation-by-platform inputs, discovery/cross-linking, search/filter model, findability validation) against §2–§9 of the artifact; the SKILL's four failure-mode anti-patterns (department structure mirrored into navigation; one shell forced on all form factors; search used to hide missing hierarchy; Vault treated as a Team-wide feed) against §1.5, §4, §7, §8, §3.4/§3.7.
- **Blocking findings:** none. Every SKILL output-contract item is present; none of the four named anti-patterns appears (§1.5 explicitly records the department-structure rule as a modelled decision; §7 explicitly inverts the search anti-pattern into a requirement; §3.4/§3.7 keep Vault Grant/Share independent of the Media Artifact tree and never reuse Vault chrome for Publication, §2.3 O-13).
- **Non-blocking findings:**
  - **IA-N1** — the destination map (§4.1) folds F-34-cond (coach-initiated external export of a minor's identifiable data, D04/legal-gated) into D-11/D-19 without a distinct row of its own, which could make its gating easy to miss at Step 4. *Remediation applied:* D-11's row now names F-34-cond explicitly as an inert-by-default sub-capability of "generate/view," cross-referenced from §2.5 O-24/O-25.
  - **IA-N2** — §5.0's "complete coverage by construction" argument (reusing J-1…J-13 for the required role walkthroughs) is a real methodological substitution for literal per-capability walkthroughs and should be flagged, not silently assumed correct. *Disposition:* recorded as an explicit **[modelled]** decision with its own justification (§5.0) rather than an unstated shortcut — this satisfies the SKILL's "distinguishes facts, assumptions, proposals" requirement; no further remediation needed, but recorded here so a later reviewer does not re-discover it as a gap.
- **Conditions:** IA-OQ-1 (D03 offline-index-freshness threshold, §9 scenario 10), IA-OQ-2 (Search tab-vs-toolbar placement, deferred to Step 4 G4), IA-OQ-3 (Vault-visibility-reason exact wording, `user-research-usability` follow-up).
- **Disposition:** **`PASS WITH CONDITIONS`** — usable as the Step 4 input; named conditions are owner-assigned, non-blocking, and do not gate a required capability's inclusion.
- **Reviewer identity:** `ux-architect` (this artifact's author, performing the SKILL-mandated self-validation pass before requesting the independent Department reviews below).
- **Downstream effect:** IA-N1 applied in this version of `information-architecture.md`; the artifact proceeds to `product-experience-lead`, `domain-workflow-architect`, D04, D02, D03, and accessibility review.
